/// SharedPreferences keys used across the app.
class PreferencesKeys {
  PreferencesKeys._();

  /// Admin fill-level alert threshold (0–100), used by settings + bin monitoring.
  static const adminFillLevelAlertThreshold = 'admin_fill_level_alert_threshold';
}
